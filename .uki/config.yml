# generated by uki | https://github.com/smokingplaya/uki
name: example script
description: example script, automaticly generated by uki
authors: ["uki", "smokingplaya"]
runner: cmd

presets:
    # $ uki
    default:
        execute:
        - ls
        - echo "uki is the best!"

    # $ uki example dev
    example:
        arguments: ["type"]
        execute: ["npm run $type"]

    # $ uki test
    test:
        execute: ["node test.ts"]

    empty:
        arguments: ["eskiit", "hi", "papa"]
        execute: ["echo 'helo'"]